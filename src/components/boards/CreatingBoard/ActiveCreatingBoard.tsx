import * as React from 'react';
// import { connect } from 'react-redux'

class ActiveCreatingBoard extends React.Component{
    render(){
        return(
            <div>This is ActiveCreatingBoard</div>
        );
    }
}

export default ActiveCreatingBoard;